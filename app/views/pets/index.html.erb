<div class="container">
<h1>Lost Pets</h1>

<ul>
  <% @pets.each do |pet| %>
    <li>
      <%= link_to pet.name, pet_path(pet) %> found <%= pluralize(pet.found_days_ago, 'day')%> ago
    </li>
  <% end %>
</ul>

<%= link_to 'Report pet', new_pet_path, class: 'btn btn-primary'%>

</div>
